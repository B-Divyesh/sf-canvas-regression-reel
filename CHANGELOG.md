# Changelog

## Unreleased

- Add the browser-safe comparison entry used by the isolated `/demo` playground.
- Add claim registry coverage, clean-package consumer verification, route metadata,
  designed 404 handling, and restrictive static-site headers.
- Replace the inline comparison with a persistent sample sandbox that can reset
  without changing non-demo browser storage.
- Version the documentation service-worker precache from each production build,
  fingerprint responsive hero images, and use immutable caching only for
  fingerprinted assets.

## 0.1.0 — 2026-08-28

- First release: seeded Playwright replay, tolerant canvas comparison, masked
  self-contained HTML report, in-browser input recorder, and documentation demo.
